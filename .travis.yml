branches:
  only:
    - master
    - dev
notifications:
  email:
    on_success: always
    on_failure: always
language: python
python:
  - "3.5"
install: true
script:
  - python3 -m unittest discover -v -s tests
